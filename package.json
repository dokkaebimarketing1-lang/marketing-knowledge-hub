{
  "name": "marketing-knowledge-hub",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro",
    "test:content": "node --test \"tests/*.test.mjs\" \"tests/**/*.test.mjs\"",
    "content:audit": "node scripts/content-audit.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "verify": "npm run test:content && npm run content:audit && npm run check && npm run build && npm run verify:build"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.1.6",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "yaml": "^2.8.0"
  }
}
